#include <stdio.h>
#include <stdint.h>

int main(){

     int len=20;
	uint8_t myNumbers[len]={25,50,100};

	uint32_t length_of_array=sizeof(myNumbers)/sizeof(uint8_t);
	printf("%d \n" ,length_of_array);

	for (uint32_t i=0;i<10;i++)
	{
		printf("%d \n" ,myNumbers[i]);
	}


	return 0;
}

